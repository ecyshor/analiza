# landing (Hugo Static Site)

## Purpose
Marketing and documentation site for analiza.dev.

## Workflow
- Built with Hugo (`hugo.yaml`)
- Content in `content/`, layouts in `layouts/`
- Deploy via Netlify

## Customization
- Themes in `themes/`
- Static assets in `static/`, `public/`
